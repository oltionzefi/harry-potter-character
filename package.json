{
  "name": "@oltionzefi/harry-potter-character",
  "version": "0.0.1",
  "description": "Get a random character from Harry Potter books",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "npx tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "commander": "^9.4.1",
    "figlet": "^1.5.2",
    "kleur": "^4.1.5"
  },
  "devDependencies": {
    "@types/figlet": "^1.5.5",
    "@types/node": "^18.11.17",
    "rimraf": "^3.0.2",
    "typescript": "^4.9.4"
  }
}
